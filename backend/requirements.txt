# ---------- Web framework ----------
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ---------- Config / validation ----------
pydantic==2.9.2
pydantic-settings==2.5.2

# ---------- Database ----------
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0
pgvector==0.3.4

# ---------- LangGraph / LangChain ----------
langgraph==0.2.34
langgraph-checkpoint-postgres==2.0.1
langchain==0.3.3
langchain-core==0.3.10
langchain-google-genai==2.0.1

# ---------- Redis / caching / reliability ----------
redis==5.0.8
tenacity==9.0.0

# ---------- Auth ----------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ---------- Email (password reset via Resend) ----------
resend==2.4.0

# ---------- PDF export ----------
weasyprint==62.3

# ---------- Observability ----------
langsmith==0.1.129
structlog==24.4.0

# ---------- HTTP client (for OpenWeather, Tavily, OpenRouteService, LocationIQ, mock flight service) ----------
httpx==0.27.2

# ---------- Testing / dev (safe to keep in prod image, small footprint) ----------
pytest==8.3.3
pytest-asyncio==0.24.0
